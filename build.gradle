task wrapper(type: Wrapper) {
    gradleVersion = "3.2"
}

System.setProperty("BASE_IMAGE", "codingcorp-docker.pkg.coding.net/registry/release/ubuntu:20201211.1")
buildscript {
    repositories {
        mavenLocal()
        maven {
            url "https://mirrors.cloud.tencent.com/nexus/repository/maven-public/"
        }
        maven {
            url "https://codingcorp-maven.pkg.coding.net/repository/registry/maven-releases/"
            credentials {
                username = System.getenv('REGISTRY_USER') ?: 'dt_uQFZxOtEdV'
                password = System.getenv('REGISTRY_PASSWORD') ?: 'nDZT7n47irtOEkOSj5ps2hhocFwcaP'
            }
        }
    }

    dependencies {
        classpath "net.coding.common:coding-plugin-collection:1.1.4"
    }
}


subprojects {
    apply plugin: "coding-plugin"

/*    dependencies {

        ext {
            springBootVersion = '1.4.7.RELEASE'
            protobufVersion = '3.3.0'
            grpcVersion = '1.22.0'
            quartzSchedulerVersion = '2.2.2'

            commonVersion = '20200928.1'
        }
        compile 'org.projectlombok:lombok:1.16.16'
        compile 'org.apache.commons:commons-lang3:3.4'
        compile 'com.zaxxer:HikariCP:2.4.5'
        compile 'org.mariadb.jdbc:mariadb-java-client:2.6.2'
        compile 'org.springframework.boot:spring-boot-starter-jdbc:1.4.7.RELEASE'
        compile group: 'com.github.pagehelper', name: 'pagehelper', version: '5.0.2'
        compile group: 'org.mybatis.spring.boot', name: 'mybatis-spring-boot-starter', version: '1.3.5'
        compile 'net.coding.common:xxl-job-core:20201012.1'
        compile 'io.sentry:sentry-logback:3.0.0'
        compile 'org.jsoup:jsoup:1.11.3'

        compile "net.coding.common:base:${commonVersion}"
        compile "net.coding.common:util:${commonVersion}"
        compile "net.coding.common:annotation:${commonVersion}"
        compile "net.coding.common:vendor:${commonVersion}"
        compile "net.coding.common:eventbus:${commonVersion}"
        compile "net.coding.common:i18n:${commonVersion}"
        compile "net.coding.common:rabbitmq:${commonVersion}"

        //proto
        compile 'net.coding.proto:project:45ba5ea37e7a8ad2c5099c7cd4ed45ee50037fe9'
        compile "net.coding.proto:wiki:20201113.1"
    }*/

}



